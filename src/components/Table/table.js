import React from 'react'
import classname from 'classnames'
import './_styles.scss'

const Table = (props) => {
    const {
        columns,
        data
    } = props
    return (
        <table></table>
    )
}

export default Table