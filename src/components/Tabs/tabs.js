import React from 'react'
import classname from 'classnames'
import './_styles.scss'

const Tabs = (props) => {   
    const {
        className
    } = props
    const classes = classname('tabs', className, {
        
    })
    return (
        <div></div>
    )
}

export default Tabs